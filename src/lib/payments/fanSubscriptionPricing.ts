// FanSubscription.amount's schema comment says "minimum 50" — ZAR-based,
// matching this app's convention of pricing everything in a ZAR base
// figure and converting for display/payment (src/lib/pricingConfig.ts).
export const FAN_SUBSCRIPTION_MIN_ZAR = 50;
